'use client';
import { FormEvent, useState } from 'react';
const API=process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:3001/api/v1';
export default function Report(){ const [loc,setLoc]=useState<{lat:number,lng:number}|null>(null); const [msg,setMsg]=useState(''); const [busy,setBusy]=useState(false);
 const gps=()=>navigator.geolocation.getCurrentPosition(p=>setLoc({lat:p.coords.latitude,lng:p.coords.longitude}),()=>setMsg('No fue posible obtener GPS. Activa ubicación e inténtalo de nuevo.'),{enableHighAccuracy:true,timeout:12000});
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault(); if(!loc){setMsg('Primero comparte tu ubicación.');return;} setBusy(true); const f=new FormData(e.currentTarget); const payload={type:f.get('type'),priority:f.get('priority'),lat:loc.lat,lng:loc.lng,address:f.get('address'),description:f.get('description'),peopleAffected:Number(f.get('peopleAffected')||0),peopleTrapped:Number(f.get('peopleTrapped')||0),contactPhone:f.get('contactPhone')}; try{const r=await fetch(`${API}/incidents`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)}); const j=await r.json(); if(!r.ok) throw new Error(j.message||'Error'); setMsg(`✅ Reporte ${j.public_id} registrado${j.potentialDuplicate?' · El sistema detectó otro reporte cercano y será revisado.':''}`); e.currentTarget.reset();}catch(err:any){setMsg(`No se pudo registrar: ${err.message}`)} finally{setBusy(false)}}
 return <main className="wrap"><div className="card"><h1>🆘 Reportar emergencia</h1><p>Comparte primero tu ubicación GPS. No necesitas crear una cuenta.</p><button className="btn danger" onClick={gps}>{loc?'✓ Ubicación capturada':'📍 Compartir mi ubicación'}</button>{loc&&<p className="muted">GPS recibido con precisión del dispositivo.</p>}<form onSubmit={submit}><label>Tipo</label><select name="type" required><option value="PEOPLE_TRAPPED">Personas atrapadas</option><option value="INJURED_PERSON">Persona herida</option><option value="BUILDING_COLLAPSE">Edificación colapsada</option><option value="BUILDING_DAMAGE">Vivienda/edificación dañada</option><option value="FIRE">Incendio</option><option value="GAS_LEAK">Fuga de gas</option><option value="MEDICAL_NEED">Necesidad médica</option><option value="WATER_NEED">Necesidad de agua</option><option value="FOOD_NEED">Necesidad de alimentos</option><option value="ROAD_BLOCKED">Vía bloqueada</option><option value="LANDSLIDE">Deslizamiento</option><option value="OTHER">Otra</option></select><label>Prioridad percibida</label><select name="priority"><option value="CRITICAL">Crítica: vidas en riesgo inmediato</option><option value="HIGH">Alta</option><option value="MEDIUM" defaultValue="MEDIUM">Media</option><option value="LOW">Baja</option></select><label>Dirección / referencia</label><input name="address" placeholder="Barrio, calle, edificio o referencia"/><label>Descripción</label><textarea name="description" rows={4} placeholder="¿Qué está ocurriendo?"/><div className="grid"><div><label>Personas afectadas</label><input name="peopleAffected" type="number" min="0" defaultValue="0"/></div><div><label>Personas atrapadas</label><input name="peopleTrapped" type="number" min="0" defaultValue="0"/></div></div><label>Teléfono de contacto</label><input name="contactPhone" inputMode="tel"/><button disabled={busy} className="btn danger" type="submit">{busy?'Enviando…':'Enviar reporte'}</button></form>{msg&&<p className="alert">{msg}</p>}</div></main> }
