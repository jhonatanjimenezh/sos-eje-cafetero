# Next implementation sequence

1. Offline PWA storage and capture.
2. SecureEnvelope client.
3. Batch sync endpoint + receipts.
4. End-to-end offline → online test.
5. WebRTC peer relay spike.
6. Operational map layers/cache.
7. Production AWS hardening in parallel.

The first demo target is: create SOS in airplane mode, close/reopen the PWA, recover connectivity, synchronize once, and observe exactly one incident in the command center.
